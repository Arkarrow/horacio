name: CI / CD

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
    TWILIO_ACCOUNT_SID: 'SKd8a853b0b2fb7aec1300ad762f9eecbf'
    TWILIO_AUTH_TOKEN: '4081e66314d65f6fc2812a150ddea394'
    TWILIO_API_SECRET: 'p2XUZrQJlycUtulAQ8Yw1Dny5ZLmQMXz'

permissions:
  contents: read

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      #- uses: stefanoeb/eslint-action@1.0.2
    
  build:
    needs: eslint
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag my-horacio:$(date +%s)
  
  send:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: 'Sending SMS Notification'
        uses: twilio-labs/actions-sms@v1
        with:
          FROM_PHONE_NUMBER: '+14342150291'
          TO_PHONE_NUMBER: '+33783601717'
          message: 'Salut bg le build de my-horacio est sur le repos'